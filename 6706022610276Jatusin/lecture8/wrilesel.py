num_dayts = int(input('for how many days do you have sales?'))
for count in range (1,num_days = 1):
    sales = float(input(f'Enter the sales for day#{count}:'))
    sales.write(str(sales)+'\n')
print('Data written to sales.txt')